---
- name: download and intall apps
  hosts: localhost
  vars:
    apps:
      - 1password
      - amazon-workspaces
      - firefox
      - google-chrome
      - intellij-idea-ce
      - pycharm-ce
      - slack
      - zoom
  tasks:
    - name: loop through app installs
      homebrew_cask:
        name: "{{ item }}"
      loop: "{{ apps }}"
